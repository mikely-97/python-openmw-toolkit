"""addon.types.npc — NPC builder helpers (re-exported for direct use)."""
# NPC building is handled directly in AddonBuilder.add_npc().
# This module is a placeholder for future extended NPC editing utilities.

class NpcBuilder:
    """Placeholder — NPC editing is done via AddonBuilder.add_npc()."""
    pass
